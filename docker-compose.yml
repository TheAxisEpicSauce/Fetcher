# docker-compose.yml
services:
  composer:
    image: composer:latest
    volumes:
      - .:/app
    entrypoint: ['composer', '--ignore-platform-reqs']

  php:
    build:
      context: .
      dockerfile: php.dockerfile
    container_name: fetcher-php
    volumes:
      - .:/app
    working_dir: /app
    depends_on:
      - mongodb
      - mysql

  phpunit:
    build:
      context: .
      dockerfile: php.dockerfile
    container_name: fetcher-phpunit
    volumes:
      - .:/app
    working_dir: /app
    entrypoint: ['vendor/bin/phpunit']

  mysql:
    image: mysql:5.7.29
    container_name: fetcher-mysql
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: db_app
      MYSQL_ROOT_PASSWORD: p0epsteen
    ports:
      - 3307:3306

  mongodb:
    image: mongo
    container_name: fetcher-mongodb
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: p0epsteen
    ports:
      - 27018:27017
